enum EnterProp {
  circle,
  titleText,
  mainText,
  desc,
  next,
}

enum ExitProp {
  scale,
  color,
}

enum EnterHomeProps {
  title,
  searchField,
  filter,
  tabs,
  list,
}

enum EnterLikedProps {
  title,
  list,
}

enum EnterCartProps {
  title,
  list,
  total,
}
